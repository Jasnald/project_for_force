{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Main Analysis",
            "type": "shell",
            "windows": {
                "command": "matlab -batch \"addpath(genpath('Src')); run('Src/main_analysis.m');\""
            },
            "linux": {
                "command": "matlab -batch \"addpath(genpath('Src')); run('Src/main_analysis.m');\""
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}